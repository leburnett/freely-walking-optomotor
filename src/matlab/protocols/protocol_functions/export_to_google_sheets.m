function export_to_google_sheets(params)
    % Google Form Response URL (Make sure it ends with /formResponse)
    googleFormURL = 'https://docs.google.com/forms/d/e/1FAIpQLSf_0TxSucXgELS500JUtflsUPLQ8AvML_yo5QP1wIaVlfy7JQ/formResponse';
    % Use weboptions to set character encoding
    options = weboptions('MediaType', 'application/x-www-form-urlencoded', 'Timeout', 30);
    % Submit data using name-value pairs instead of struct
    webwrite(googleFormURL, ...
        'entry.975081033', params.Date, ...
        'entry.1659164285', params.Time, ...
        'entry.1681987389', params.Experimenter, ...
        'entry.1804090377', params.Strain, ...
        'entry.315771615', params.Age, ...
        'entry.1034952451', params.Sex, ...
        'entry.788375644', params.LightCycle, ...
        'entry.306455063', params.Protocol, ...
        'entry.1284387345', params.NotesStart, ...
        'entry.1661651524', params.NotesEnd, ...
        options); % Include options for correct encoding
    disp('Data successfully exported to Google Sheets.');
end